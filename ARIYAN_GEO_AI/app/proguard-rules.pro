# Release minification is currently disabled (minifyEnabled false) for
# this vertical-slice build, so no rules are required yet. If minification
# is turned on later, Chaquopy's own consumer-rules.pro (bundled in its
# AAR) already protects the Python runtime; add app-specific keep rules
# here as needed at that point.
